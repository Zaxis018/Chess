class pawn:
    def __init__(self, name, position):
        self.name = name
        self.position = position

    def is_valid_move(self):
        if piece='wP':
            if cur_pos.y_pos=2 and (next_pos==(cur_pos.x,cur_pos.y+1) or next_pos==(cur_pos.x,cur_pos.y+1)):
            